#!/usr/bin/env bash

if [ ! -d $0../node_modules/ ]; then
    echo "node_modules missing - you must first install all node packages by executing 'npm install' in the root directory of SnowFS"
    exit 1
fi

node -r $0/../../node_modules/ts-node/register $0/../../main.ts $@